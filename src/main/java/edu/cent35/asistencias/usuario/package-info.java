/**
 * Modulo de Usuarios del sistema (Superadmin de institucion y Administradores).
 * <p>
 * Cubre RF-01 (login), RF-02 (gestion de contrasenas) y RF-03 (control por rol).
 * El docente NO es un usuario del sistema (no tiene login).
 */
package edu.cent35.asistencias.usuario;
